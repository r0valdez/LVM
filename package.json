{
  "name": "lan-video-meeting",
  "version": "1.0.0",
  "description": "LVM Desktop App",
  "main": "electron/main.js",
  "type": "commonjs",
  "author": "",
  "scripts": {
    "start": "electron .",
    "prepare-cache": "node scripts/prepare-electron-cache.js",
    "prepare-offline": "node scripts/prepare-offline-build.js",
    "build": "node scripts/build-offline.js",
    "build:win": "node scripts/build-offline.js --win",
    "build:dir": "node scripts/build-offline.js --dir",
    "build:offline": "node scripts/build-offline.js --dir",
    "dist": "node scripts/build-offline.js --publish=never",
    "prebuild": "node scripts/prepare-electron-cache.js"
  },
  "dependencies": {
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-builder": "^24.9.1"
  },
  "build": {
    "appId": "com.lvm.app",
    "productName": "LVM",
    "electronVersion": "31.7.7",
    "electronDist": "./node_modules/electron/dist",
    "directories": {
      "output": "dist"
    },
    "files": [
      "electron/**/*",
      "renderer/**/*",
      "package.json"
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    },
    "portable": {
      "artifactName": "${productName}-${version}-portable.${ext}"
    }
  }
}

